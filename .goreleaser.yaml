project_name: iptv-proxy

builds:
  - binary: iptv-proxy
    env:
      - CGO_ENABLED=0
    flags:
      - -mod=vendor
      - -a
    goos:
      - windows
    goarch:
      - amd64

archives:
  -
   format_overrides:
     - goos: windows
       format: zip

release:
  github:
    owner: MatAnzPL
    name: iptv-proxy
  draft: false
